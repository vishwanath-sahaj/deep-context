[project]
name = "deep-context"
version = "0.1.0"
description = "A Python agent project for creating Documentation"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "google-adk>=1.23.0",
    # "mcp-server-playwright>=0.0.1", # Placeholder for MCP
]

[project.optional-dependencies]
dev = [
    "black>=24.0.0",
]

[tool.uv]
# optimal settings for uv
package = true
