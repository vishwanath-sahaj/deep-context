# Required
CLAUDE_API_KEY=

# Optional overrides (defaults shown)
CLAUDE_CHAT_MODEL=claude-haiku-4-5
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# Optional: override the FAISS index directory (default: <repo>/.deep-context-index)
# INDEX_DIR=./my-custom-index